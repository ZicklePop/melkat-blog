---
const cx = {
  a: 'no-underline hover:underline text-cerulean-700 dark:text-cerulean-500',
}

const { domain } = Astro.props
---
<a class={cx.a} href={`/d/${domain}`}>{domain?.replace('www.', '')}</a>
