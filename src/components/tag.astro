---
import { cxs } from '../consts/config'

export interface Props {
  tag: string
}

const { tag } = Astro.props
---

<a class={cxs.a} href={`/t/${tag}`} aria-label={`Posts tagged #${tag}`}
  >#{tag}
</a>
