---
import Icon from './icon.astro'
import { cxs } from '../consts/config'

export interface Props {
  tag: string
}

const { tag } = Astro.props
---

<a
  class={`${cxs.aIcon} gap-0`}
  href={`/t/${tag}`}
  aria-label={`Posts tagged #${tag}`}
  ><Icon type="tag" />{tag}
</a>
