---
import formatDate from "@/src/lib/format-date";

export interface Props {
	datetime: Date;
}

const { datetime } = Astro.props;
---

<time class="dt-published" datetime={datetime.toISOString()}>
  {formatDate(datetime)}
</time>
