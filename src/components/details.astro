---
import Domain from './domain.astro'
import Date from './date.astro'

const cx = {
  details: 'uppercase text-sm my-4',
}

const {
  date,
  link,
  seperator = ' • ',
} = Astro.props
---
<p class={cx.details}>
  {link && <Domain {link} /> }
  {link ? seperator : ''}
  <Date {date} />
</p>
