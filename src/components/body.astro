---
import { cxs } from '../consts/config'

export interface Props {
  truncate?: boolean
}

const { truncate = false } = Astro.props
---

<div class={`${cxs.prose} my-4 ${truncate ? 'p-summary' : 'e-content'}`}>
  <slot />
</div>
