---
import { Image } from '@astrojs/image/components'

const cx = {
  img: 'h-16 w-auto rounded ml-2 mb-2 float-right',
}

const { title, cover } = Astro.props
---

<Image
  src={import(`../../public${cover}`)}
  alt={title}
  aspectRatio="16:9"
  height={164}
  class={cx.img}
/>
