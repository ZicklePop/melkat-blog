---
import { Image } from 'astro:assets'

const cx = {
  img: 'rounded-lg mb-3 u-photo',
}

const { cover, coverAlt } = Astro.props
---

<Image
  alt={coverAlt}
  class={cx.img}
  src={cover}
  densities={[1.5, 2, 3]}
  width={656}
  height={369}
/>
