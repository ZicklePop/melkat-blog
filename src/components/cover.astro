---
import { Image } from 'astro:assets'

export interface Props {
  title: string
  cover: string
}

const cx = {
  img: 'rounded-lg aspect-video object-cover mb-3',
}

const { title, cover } = Astro.props
---

<Image alt={title} class={cx.img} src={cover} width={560} height={315} />
